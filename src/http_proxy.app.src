{application, http_proxy,
 [
  {description, ""},
  {vsn, "1"},
  {registered, []},
{modules, [http_proxy_app, http_proxy_sup, toppage_handler]},
  {applications, [
                  kernel,
                  stdlib,
		  crypto,
 		asn1,
		public_key,
		ssl,
		ranch,
		cowlib,
                  cowboy,
		  ibrowse
                 ]},
  {mod, { http_proxy_app, []}},
  {env, []}
 ]}.
